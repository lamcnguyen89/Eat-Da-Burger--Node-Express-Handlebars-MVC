<li>
	{{name}}
	<br>
	<button class="change-eat m-1" data-id="{{id}}" data-neweat="{{eat}}">
		{{#if eat}}REMOVE EATEN BURGER{{else}}SEND BURGER TO CUSTOMER {{/if}}
	</button>
	<br>
	<button class="delete-burger" data-id="{{this.id}}">DELETE</button>
</li>
