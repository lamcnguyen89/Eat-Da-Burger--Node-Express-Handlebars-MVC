<h1>Sandwich Samsara</h1>

<h2>Burgers that Crave to be Consumed!</h2>

<ul>
  {{#each burgers}}
    {{#unless sleepy}}
      {{> burgers/burger-block sleep=true}}
    {{/unless}}
  {{/each}}
</ul>

<h2>Burgers that have Fulfilled Their Life Pupose</h2>

<ul>
  {{#each burgers}}
    {{#if sleepy}}
      {{> burgers/burger-block sleep=false}}
    {{/if}}
  {{/each}}
</ul>

<h2>Add a Burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="ca">Burger Name:</label>
    <input type="text" id="ca" name="name">
  </div>

  <div class="form-group">
    <label for="sleepy">To be Devoured or Not?</label><br>
    <input type="radio" name="sleepy" value="1" checked> TO BE DEVOURED..<br>
    <input type="radio" name="sleepy" value="0"> ALREADY DEVOURED!
  </div>

  <button type="submit">Add Burger</button>
</form>
